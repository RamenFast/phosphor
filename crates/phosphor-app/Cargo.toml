[package]
name = "phosphor-app"
description = "The phosphor binary: egui shell (scope canvas as a raw wgpu pass), transport, playlist, kit editor, studio panel, MPRIS, control socket"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "phosphor"
path = "src/main.rs"

[dependencies]
phosphor-proto.workspace = true
