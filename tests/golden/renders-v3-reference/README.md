# v3 look references (NOT gates)

Final frames from v3's OfflineFrameRenderer (Cairo, pristine
Python engine) over short fixture streams — human-eye anchors
for 'does v4 still look like Phosphor'. The v4 beam is the GL
shaders' analytic Gaussian, so v4 frames legitimately differ
from these Cairo strokes in beam profile; the binding gates
are the cross-renderer snapshot tests in
crates/phosphor-render-gpu/tests/. Regenerate: the script in
the wave-1 step-4 commit message / session log.
