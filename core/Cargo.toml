[package]
name = "phosphor-core"
version = "3.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Native signal core for Phosphor: samples -> beam segments"

[lib]
name = "phosphor_core"
crate-type = ["cdylib"]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
